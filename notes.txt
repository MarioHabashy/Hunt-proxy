fixes:
when send websocket in repeater it dont appear in browser 
repeater group send parallel


** in bypass access control need to try X-Forward-For with internal IPs like 192.168.1.100
** in bypass access control need to apply this techniqe GET /robots.txt/..;/admin/ or /cart.jsp/..;/admin/  any valid page then /..;/403 page
** fix JS Miner
** fix localhost testing
** add JSON beatufier in all tabs
** fix flow in attack surface tab as cant add POST requests

*d* fix suddenly close tool
*d* fix freeze and not responding with large traffic if param miner and JS miner is the cause
*d* add in repeater , intercept option clean request which remove every parameter and headers in request which dont effect the response
*d* when choose many URLs in http history tab then copy need all seclected copied not only the first one
*d* add intercept the response for this reuqest in intercept tab >>> now need to test and test when tur on responses as i find one response only intercepeted when on responses
*d* add in repeater/history check methods which will check http methods and olso headers which overrite method X-HTTP-Method-Override 
*d* add in repeater/history option like this https://github.com/zigoo0/Pemburu/tree/master which try to find dev like env for this endpoint
*d* add in repeater/history option test use polyglot to test many vulners in one input in one request SSTI XSS SQLI BXSS .. etc
*d* improve tool in these labs https://labs.cai.do/ specially RXSS llab as not reconginze company parameter when var company = 'ss' can detect but if var company = '' empty cant detect
*d* in tools runner in content bruteforce need to add wordlists for technologies not one wordlist as in tomcate 
    so need to make is dynamically to find them like find ~/SecLists/Discovery | grep -i tomcat .... techns from wad and headers
*d* fix error in detect tech when bruteforce

scanner tab :
    need to make interact.sh cli and auto used to detect blind one



jwt tab:
    change matched logic to be Status code not length

JS miner:
    fix 

JSAnalysisWorker error for http://localhost:5173/MedRepPlanner.jsx
Traceback (most recent call last):
  File "/home/mario/Desktop/hunt/hunt_gui/js_miner_tab.py", line 664, in run
    "summary":    summary,
                      ^^^^^
  File "/usr/lib/python3.12/concurrent/futures/_base.py", line 458, in result
    raise TimeoutError()
TimeoutError



SSTI scan:
    continue



